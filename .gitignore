# secrets
.env
.env.local

# node
node_modules/
npm-debug.log*

# os / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# weft editor runtime
wfed.log
wfed.pid

# local scratch screenshots (ad-hoc hidden .png files at the repo root)
/.*.png

# NOTE: generated game art (games/*/assets/*.png) IS committed so the hosted
# portal shows real illustrations. Regenerate with `weft art <game> --generate`.
